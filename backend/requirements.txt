# Resume Parser - Minimal Production Requirements
# ================================================

# Web Framework
Flask==3.1.3
Werkzeug==3.1.8

# PDF to Image Conversion
pdf2image==1.17.0
# On Windows: install Poppler separately (see SETUP_GUIDE.md or MANUAL_SETUP.md)
# Remove python-poppler-qt5 because it requires Qt build tools; pdf2image uses the
# poppler "pdftoppm" binary which should be installed and available on PATH.

# Image Processing
Pillow==12.2.0

# OCR Engine
paddleocr==2.7.0.3
paddlepaddle==2.6.2

# Object Detection (Layout Analysis)
ultralytics==8.4.38
torch==2.11.0

# NLP / Named Entity Recognition
spacy==3.7.2

# Computer Vision (required by paddleocr)
opencv-python==4.6.0.66

# Utilities (commonly needed)
numpy==1.26.4
Pillow==12.2.0

# BERT NER Dependencies
transformers>=4.38.1
joblib>=1.3.2
scikit-learn>=1.4.1.post1

# Note: python-poppler-qt5 is a Windows-friendly alternative to system poppler
# If on Linux/Mac, consider: python-poppler or apt-get install poppler-utils
